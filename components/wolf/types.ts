// Tipos para el componente Wolf Guide
export type WolfState = "welcome" | "about" | "skills" | "projects" | "contact" | "experience" | "idle"

export interface WolfTranslations {
  welcome: string
  about: string
  skills: string
  projects: string
  contact: string
  experience: string
  idle: string
}
